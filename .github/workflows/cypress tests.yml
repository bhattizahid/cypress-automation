name: Cypress test 
on: [push]
jobs:
    cypress-run:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v3
        - name: Cypress run
          uses: cypress-io/github-action@v5
          with:
            node-version: '16'
        - name: Install dependencies
          run: npm ci
          working-directory: C:\Users\ZAHID NAWAZ\CypressAutomation